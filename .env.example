# Copy to .env (gitignored). Loaded automatically by load_config (python-dotenv).
# Used for the LLM fallback sentence generator, the fallback TTS, and v1.1 image
# word-extraction — one key for all three (same OpenAI-compatible client, D4).
OPENAI_API_KEY=sk-your-key-here

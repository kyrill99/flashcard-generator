Anki flashcards [/flashcards](tg://bot_command?command=flashcards) from image or text

- Input:
- Image with either:
- words (in target language)
- text with underlined/marked words (in target language)
- text
- words in target language
- Output:
- Anki flashcards with:
- default: Word, sentence, audio (type-in)
- later: should be able to specify what want exactely

PRD binding:

- Anki flashcards should be directely imported into Ankii and appended to choosen deck

PRD ideas (room for improvement & research)

- Input trough app or for example telegram bot, what could be most user friendly option? start with the simpler case (app can also be done when core engine already works)
- should run on a server, use cheapest possible but still good enough LLM. How to have minimal cost and acheave very good results still?
- one idea: have mini model ship with the app (or call free LLM API)
- Should be extremely user friendly (mabe also use design agent like claude design, google stitch ...)
